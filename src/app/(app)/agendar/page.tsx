import { EscolherTipoView } from '@/features/m04/usuaria/escolher-tipo-view'

/** U1 · Escolher tipo de sessão — entrada do fluxo de marcação (M04). */
export default function AgendarPage() {
  return <EscolherTipoView />
}
